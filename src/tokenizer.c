/*
 * tokenizer.c — BPE tokenizer (stub).
 *
 * Replaced by vendored upstream tokenizer in Phase 1. The full Phase 1
 * port lifts the `Tokenizer` struct and encode/decode entry points from
 * vendor/llama2.c (see vendor/llama2.c/UPSTREAM_SHA for the pinned rev).
 */
