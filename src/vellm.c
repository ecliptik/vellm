/*
 * vellm.c — main inference driver
 *
 * Ported from karpathy/llama2.c run.c — replace in Phase 1.
 *
 * Phase 0 stub: the real port lives in Phase 1 where we fork
 * vendor/llama2.c/run.c into this file, apply the minimum DOS diff
 * (explicit "rb" fopen, setvbuf, 32-bit ftell audit, no mmap), and
 * wire up the cross-DJGPP build. See PLAN.md Phase 1 for details.
 */

#include <stdio.h>

int main(void) {
    /* stub: Phase 1 replaces this with the ported inference loop */
    return 0;
}
