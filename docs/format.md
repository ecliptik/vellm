# `.q8` file format

TBD (Phase 2).

This document will describe the int8-quantized checkpoint format produced
by `tools/quantize.py` and consumed by `src/quant.c`. The header layout
stub lives in `src/quant.h` as `struct q8_header`.
