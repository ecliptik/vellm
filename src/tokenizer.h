/*
 * tokenizer.h — BPE tokenizer interface (stub).
 *
 * Replaced by vendored upstream tokenizer in Phase 1. This stub only
 * exists so early scaffolding code compiles.
 */
#ifndef VELLM_TOKENIZER_H
#define VELLM_TOKENIZER_H

#ifdef __cplusplus
extern "C" {
#endif

/* Phase 1 will lift the Tokenizer struct + encode/decode from upstream. */

#ifdef __cplusplus
}
#endif

#endif /* VELLM_TOKENIZER_H */
